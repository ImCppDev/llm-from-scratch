[project]
name = "derivatives-and-gradients"
version = "0.1.0"
description = "Lesson 00.00: derivatives and gradients by hand"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
